# Отправить почту (SMTP)

Данный шаг предназначен для отправки почтовых сообщений по протоколу SMTP. Для выполнения отправки используется класс `SmtpClient`, предоставляемый платформой .NET Core.

Шаг позволяет отправлять электронные письма с настраиваемыми параметрами подключения к SMTP-серверу и содержимым сообщения.

## Параметры шага

В таблицах ниже используются унифицированные типы параметров:
- `string` — строковое значение  
- `number` — числовое значение  
- `boolean` — логическое значение (`true / false`)

### Параметры подключения к SMTP

| Параметр         | Тип     | Обязательный | Параметризация | По умолчанию | Описание                                         |
|------------------|---------|--------------|----------------|--------------|--------------------------------------------------|
| **Host**         | string  | да           | да             | —            | Имя или адрес SMTP-сервера                       |
| **Port**         | number  | да           | да             | —            | Порт SMTP-сервера                                |
| **Пользователь** | string  | да           | да             | —            | Имя пользователя для авторизации на SMTP-сервере |
| **Пароль**       | string  | да           | да             | —            | Пароль для авторизации на SMTP-сервере           |
| **Таймаут**      | number  | нет          | да             | `0`          | Таймаут ожидания операции отправки в секундах    |
| **Включить SSL** | boolean | нет          | нет            | `false`      | Использовать защищённое соединение (SSL)         |

### Параметры почтового сообщения

| Параметр                | Тип     | Обязательный | Параметризация | По умолчанию | Описание                                    |
|-------------------------|---------|--------------|----------------|--------------|---------------------------------------------|
| **Отправитель**         | string  | да           | да             | —            | Почтовый адрес отправителя                  |
| **Получатель**          | string  | да           | да             | —            | Почтовый адрес получателя                   |
| **Копия (CC)**          | string  | нет          | да             | —            | Почтовый адрес получателя копии             |
| **Скрытая копия (BCC)** | string  | нет          | да             | —            | Почтовый адрес получателя скрытой копии     |
| **Тема**                | string  | да           | да             | —            | Тема письма                                 |
| **Текст**               | string  | нет          | да             | —            | Содержимое письма (текст или HTML)          |
| **Тело письма в HTML**  | boolean | нет          | нет            | `false`      | Признак того, что тело письма задано в HTML |

## Параметризация

В полях, поддерживающих параметризацию, могут использоваться шаблоны следующего вида:
```javascript  
{% raw %}
`Some text {{javaScriptExpression}}`
{% endraw %}
```

JavaScript-выражения, заключённые в двойные фигурные скобки, вычисляются во время выполнения процесса. Внутри таких выражений доступен следующий контекст:

- **_parameters** — параметры процесса, переданные при его запуске.
- **_data** — данные, полученные на предыдущих шагах процесса.

В выражениях шаблонов **не поддерживаются** обращения к базе данных и другие асинхронные операции (включая использование `await`). Допускаются только синхронные JavaScript-выражения, работающие с уже доступными данными процесса.

### Пример

Тема письма, информирующего о том, что заказ просрочен. Предполагается, что в процессе существует шаг с именем `order`, содержащий информацию о заказе.

```
{% raw %}
Заказ № {{_data.order.number}} просрочен
{% endraw %}
```
